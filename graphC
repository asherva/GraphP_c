DECLARE @FreePercent DECIMAL(5,2);

SELECT TOP 1 @FreePercent = FreePercent
FROM OPENROWSET(
    BULK 'C:\Reports\DiskFreeSummary.csv',
    FORMAT = 'CSV',
    FIRSTROW = 2
) AS t;

PRINT @FreePercent;
